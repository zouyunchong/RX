# 独立 server 块（仅当 masion.xyz 尚无 nginx 配置时使用）
# 若已有 AI 专卖站点，请改用 deploy/nginx-location-rx.conf 片段，由 deploy.sh 自动注入。

server {
    listen 80;
    server_name masion.xyz;

    include /etc/nginx/snippets/luckin-rx.conf;
}
